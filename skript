02 suma hodnota zeny = 


-- suma nad sloupcem Hodnota pro dany rok a pohlavi Zeny
-- k existujicimu filtru vizualu potrebujeme pridat filtr nad sloupcem pohlavi

-- krok2: Funkce SUMX() scita tabulku vracenou funkci FILTER()
-- nad sloupcem Hodnota
sumx(
--krok1: Funkce FILTER() prebira tabulku filtrovanou vizualem
-- a a plikuje filtr nad sloupcem pohlavi
-- funkce FILTER() vrati tabulku
FILTER( 
        -- tabulka data je filtrovana vizualem pres sloupec rok
        data,
        data[Pohlavi] = "zeny"),
data[Hodnota])
